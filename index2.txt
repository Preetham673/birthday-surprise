<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Happy Birthday 🎉</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
<style>
/* General */
body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  color: #fff;
  overflow-x: hidden;
  overflow-y: auto;
  background: #ffafbd;
}

/* Page container */
.page {
  min-height: 100vh;
  width: 100vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  padding: 60px 20px;
  position: relative;
  gap: 30px;
}
.hidden { opacity:0; pointer-events:none; transition: opacity 1s ease; }

/* Backgrounds */
.page1-bg {
  position: fixed;
  top:0; left:0;
  width:100%; height:100%;
  background: url('page1-bg.jpg') no-repeat center center;
  background-size: cover;
  opacity: 0.8;
  z-index: 0;
}
.background-gradient {
  position: fixed;
  top:0; left:0;
  width:100%; height:100%;
  background: linear-gradient(-45deg, #ff6b6b, #ffda79, #9c88ff, #ff80a6, #a8ff78, #78ffd6, #ffaf6a, #ff6b81);
  background-size: 1600% 1600%;
  animation: rainbowBG 30s ease infinite;
  z-index: 5;
}
@keyframes rainbowBG {
  0% { background-position: 0% 50%; }
  25% { background-position: 50% 50%; }
  50% { background-position: 100% 50%; }
  75% { background-position: 50% 50%; }
  100% { background-position: 0% 50%; }
}

/* Titles */
.title { font-size:60px; text-align:center; line-height:1.2; text-shadow:0 0 20px #fff,0 0 30px #ff4d6d; }
.subtitle { font-size:26px; color:#fce4ec; margin-bottom:20px; text-align:center; text-shadow:0 0 10px rgba(0,0,0,0.5); }

/* Gallery */
.content-wrapper { display:flex; flex-direction:column; align-items:center; gap:20px; }
.gallery { display:flex; flex-wrap: wrap; gap:25px; justify-content:center; max-width:1200px; }
.photo-card { position: relative; width:260px; height:350px; cursor:pointer; overflow:hidden; border-radius:25px; box-shadow:0 10px 40px rgba(0,0,0,0.6); transition: transform 0.3s; }
.photo-card img { width:100%; height:100%; object-fit:cover; display:block; }
.photo-card .message-box {
  position:absolute; top:0; left:0; width:100%; height:100%;
  background: rgba(255,105,180,0.9);
  display:flex; align-items:center; justify-content:center;
  font-size:22px; font-weight:bold; color:#fff; text-align:center;
  opacity:0; transition: opacity 0.5s; padding:15px;
  text-shadow:0 0 8px #000;
}
.photo-card.show-message { transform: scale(1.08); }
.photo-card.show-message .message-box { opacity:1; }

/* Button */
.btn {
  margin-top: 20px;
  background: linear-gradient(90deg, #11e4e3, #3b82f6);
  border:none;
  color:#fff;
  padding:16px 28px;
  border-radius:12px;
  font-size:18px;
  cursor:pointer;
  transition: transform 0.3s, box-shadow 0.3s;
  box-shadow:0 4px 15px rgba(0,0,0,0.3);
}
.btn:hover { transform: scale(1.08); box-shadow:0 8px 20px rgba(0,0,0,0.5); }

/* Canvas for hearts */
#heartsCanvas { position:absolute; inset:0; pointer-events:none; z-index:1; }

/* Page 2 card */
.card {
  width: 80%; max-width: 700px; height: 70%;
  background: rgba(255,255,255,0.15);
  backdrop-filter: blur(10px);
  border-radius:25px; box-shadow:0 0 50px rgba(0,0,0,0.7);
  position: relative; overflow:hidden;
  display:flex; flex-direction:column; align-items:center; justify-content:center;
  z-index: 15;
}
.bday-title {
  font-size:72px;
  background: linear-gradient(90deg,#fff, #ffd700, #ff69b4, #fffff0);
  -webkit-background-clip:text; color:transparent; margin-bottom:5px;
  text-shadow:0 0 10px rgba(255,255,255,0.8);
}
.bday-sub { font-size:24px; color: rgba(255,255,255,0.9); text-shadow:0 0 5px rgba(0,0,0,0.5); }
canvas#confetti, canvas#floatingHeartsCanvas { position:absolute; inset:0; pointer-events:none; z-index:10; }
.controls { margin-top:30px; }

/* Animations */
@keyframes rainbowBG { 0%{background-position:0% 50%} 25%{background-position:50% 50%} 50%{background-position:100% 50%} 75%{background-position:50% 50%} 100%{background-position:0% 50%} }
</style>
</head>
<body>

<!-- Page 1 -->
<div class="page" id="page1">
  <div class="page1-bg"></div>
  <h1 class="title">Happy Birthday My Dear Wife 💖</h1>
  <p class="subtitle">Click each photo to reveal a message 💝</p>
  
  <div class="content-wrapper">
    <div class="gallery">
      <div class="photo-card" data-message="Thanks for coming into my life 💖"><img src="g-1.jpg" alt="Photo 1"><div class="message-box"><p></p></div></div>
      <div class="photo-card" data-message="Let’s celebrate all your birthdays together forever 🎈"><img src="g-2.jpg" alt="Photo 2"><div class="message-box"><p></p></div></div>
      <div class="photo-card" data-message="I’ll be with you till my last breath 💞"><img src="g-3.jpg" alt="Photo 3"><div class="message-box"><p></p></div></div>
    </div>
    <button class="btn" id="goNext">Next Surprise 🎁</button>
  </div>
  
  <canvas id="heartsCanvas"></canvas>
</div>

<!-- Page 2 -->
<div class="page hidden" id="page2">
  <div class="background-gradient"></div>
  <div class="card" id="card">
    <h1 class="bday-title">Happy Birthday 🎉</h1>
    <p class="bday-sub">You bring so much joy and love into my life ❤️</p>
    <canvas id="confetti"></canvas>
    <canvas id="floatingHeartsCanvas"></canvas>
    <div class="controls">
      <button class="btn" id="party">Celebrate Again 🎊</button>
    </div>
  </div>
</div>

<script>
// ---------- Page 1 Hearts ----------
const heartsCanvas = document.getElementById('heartsCanvas');
const hCtx = heartsCanvas.getContext('2d');
function resizeHeartsCanvas() { heartsCanvas.width = window.innerWidth; heartsCanvas.height = window.innerHeight; }
resizeHeartsCanvas();
window.addEventListener('resize', resizeHeartsCanvas);

let hearts=[], sparkles=[];

function spawnSparkles(count=100){
  for(let i=0;i<count;i++) sparkles.push({x:Math.random()*window.innerWidth,y:Math.random()*window.innerHeight,radius:Math.random()*2+1,alpha:Math.random(),speed:Math.random()*0.5+0.2});
}
function spawnHearts(count=15){
  for(let i=0;i<count;i++) hearts.push({x:Math.random()*window.innerWidth,y:window.innerHeight+Math.random()*100,size:Math.random()*40+30,speed:Math.random()*2+1,angle:Math.random()*Math.PI*2,rotationSpeed:Math.random()*0.05+0.01});
}
function animateHearts(){
  hCtx.clearRect(0,0,heartsCanvas.width,heartsCanvas.height);
  sparkles.forEach(s=>{
    hCtx.beginPath();
    hCtx.arc(s.x,s.y,s.radius,0,Math.PI*2);
    hCtx.fillStyle=`rgba(255,255,255,${s.alpha})`;
    hCtx.fill();
    s.y-=s.speed; s.alpha-=0.005;
    if(s.alpha<=0){ s.x=Math.random()*window.innerWidth; s.y=window.innerHeight; s.alpha=Math.random(); s.speed=Math.random()*0.5+0.2; }
  });
  for(let i=hearts.length-1;i>=0;i--){
    const heart=hearts[i]; heart.y-=heart.speed; heart.angle+=heart.rotationSpeed;
    const s=heart.size;
    hCtx.save();
    hCtx.translate(heart.x,heart.y);
    hCtx.rotate(Math.sin(heart.angle)/10);
    hCtx.fillStyle='#ff69b4';
    hCtx.font=`${s}px Arial`;
    hCtx.fillText('❤️ You',-s/2,s/2);
    hCtx.restore();
    if(heart.y+s<0) hearts.splice(i,1);
  }
  requestAnimationFrame(animateHearts);
}
spawnSparkles(); animateHearts();

// ---------- Photo overlay messages ----------
document.querySelectorAll('.photo-card').forEach(card=>{
  const messageBox = card.querySelector('.message-box p');
  messageBox.textContent = card.dataset.message;
  card.addEventListener('click',()=>{ card.classList.toggle('show-message'); if(card.classList.contains('show-message')) spawnHearts(15); });
});

// ---------- Page switch ----------
const page1=document.getElementById('page1');
const page2=document.getElementById('page2');
const goNext = document.getElementById('goNext');
goNext.addEventListener('click',()=>{
  page1.classList.add('hidden');
  setTimeout(()=>{
    page1.style.display='none';
    page2.classList.remove('hidden');
    startPage2Effects();
  },800);
});

// ---------- Page 2 Confetti & Floating Hearts ----------
const confettiCanvas = document.getElementById('confetti');
const confCtx = confettiCanvas.getContext('2d');
const heartsCanvas2 = document.getElementById('floatingHeartsCanvas');
const heartsCtx2 = heartsCanvas2.getContext('2d');
let W=window.innerWidth,H=window.innerHeight;
confettiCanvas.width=heartsCanvas2.width=W;
confettiCanvas.height=heartsCanvas2.height=H;

window.addEventListener('resize', ()=>{
  W=confettiCanvas.width=heartsCanvas2.width=window.innerWidth;
  H=confettiCanvas.height=heartsCanvas2.height=window.innerHeight;
});

let confetti=[], floatingHearts2=[];
const palette=['#ff3d00','#ffd600','#00e676','#2979ff','#d500f9','#ff80ab'];

class Confetti {
  constructor(x,y){ this.x=x; this.y=y; this.w=Math.random()*6+6; this.h=Math.random()*8+8; this.vx=Math.random()*4-2; this.vy=Math.random()*2+1; this.color=palette[Math.floor(Math.random()*palette.length)]; this.life=0; this.ttl=Math.random()*80+70; }
  update(){ this.x+=this.vx; this.y+=this.vy; this.vy+=0.03; this.life++; }
  draw(ctx){ ctx.fillStyle=this.color; ctx.fillRect(this.x,this.y,this.w,this.h); }
}

class FloatingHeart2 {
  constructor(){ this.size=Math.random()*20+25; this.x=Math.random()*W; this.y=H+this.size; this.speedY=Math.random()*0.8+0.3; this.speedX=Math.random()*0.8-0.4; this.opacity=Math.random()*0.5+0.5; this.wave=Math.random()*Math.PI*2; }
  update(){ this.y-=this.speedY; this.wave+=0.01; this.x+=Math.sin(this.wave)*0.5; if(this.y<-this.size){ this.y=H+this.size; this.x=Math.random()*W; } }
  draw(ctx){ ctx.save(); ctx.globalAlpha=this.opacity; ctx.font=`${this.size}px Arial`; ctx.fillStyle='#ff69b4'; ctx.fillText('PS',this.x,this.y); ctx.restore(); }
}

function spawnBurst(cx,cy,count=50){ for(let i=0;i<count;i++){ const c=new Confetti(cx,cy); const angle=Math.random()*Math.PI*2; const speed=Math.random()*4+2; c.vx=Math.cos(angle)*speed; c.vy=Math.sin(angle)*speed; confetti.push(c); } }

function animatePage2(){
  confCtx.clearRect(0,0,W,H);
  heartsCtx2.clearRect(0,0,W,H);
  floatingHearts2.forEach(h=>{ h.update(); h.draw(heartsCtx2); });
  for(let i=confetti.length-1;i>=0;i--){ const c=confetti[i]; c.update(); c.draw(confCtx); if(c.y>H+40||c.life>c.ttl) confetti.splice(i,1); }
  requestAnimationFrame(animatePage2);
}

document.getElementById('party').addEventListener('click',()=>spawnBurst(W/2,H/2,160));

function startPage2Effects(){
  for(let i=0;i<30;i++) floatingHearts2.push(new FloatingHeart2());
  setInterval(()=>spawnBurst(Math.random()*W*0.8+W*0.1, Math.random()*H*0.4+H*0.2, 20),1500);
  animatePage2();
}
</script>

</body>
</html>
